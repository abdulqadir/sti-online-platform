server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    location /static/ {
        root /home/sti/onlineplatform;
    }
    location / {
        proxy_pass http://unix:/run/gunicorn.socket;
    }
}
